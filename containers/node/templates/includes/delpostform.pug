div.userdelete
  div
    | Delete message
    | 
    label.link-button-wrapper
      input#fileonly(name='fileonly', type='checkbox')
      | File only
  label Пароль
    input(name='postpassword', size='8', type='password')
  input(name='deletepost', value='Delete', type='submit')
  if board.enableReporting
    input(name='reportpost', value='Report', type='submit')
